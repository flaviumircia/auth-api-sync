
# Server Configuration
server:
  address: 0.0.0.0
  port: 8080

# Spring database
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sync_auth
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate